name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-release:
    name: Build Release
    runs-on: macos-14

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Select Xcode version
      run: sudo xcode-select -s /Applications/Xcode_16.0.app/Contents/Developer

    - name: Setup Local Configuration
      run: |
        # Create Local.xcconfig from template for CI
        # CI doesn't need a real Team ID since we disable code signing
        cp ios/Config/Local.xcconfig.example ios/Config/Local.xcconfig
        # Replace placeholder with dummy value (CI uses CODE_SIGNING_REQUIRED=NO)
        sed -i '' 's/YOUR_TEAM_ID/CI_BUILD/' ios/Config/Local.xcconfig
        echo "✅ Created Local.xcconfig for CI build"

    - name: Install dependencies
      run: |
        brew install swiftlint swiftformat

    - name: Lint check
      run: cd ios && swiftlint lint --strict

    - name: Format check
      run: swiftformat --lint ios/

    - name: Run all tests
      run: |
        xcodebuild test \
          -project ios/StillMoment.xcodeproj \
          -scheme StillMoment \
          -destination 'platform=iOS Simulator,name=iPhone 17,OS=19.0' \
          -enableCodeCoverage YES \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGNING_REQUIRED=NO

    - name: Build Archive
      run: |
        xcodebuild archive \
          -project ios/StillMoment.xcodeproj \
          -scheme StillMoment \
          -archivePath ios/StillMoment.xcarchive \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGNING_REQUIRED=NO \
          -destination 'generic/platform=iOS'

    - name: Create Release Notes
      id: release_notes
      run: |
        VERSION=${GITHUB_REF#refs/tags/v}
        cat << EOF > release-notes.md
        # Still Moment v$VERSION

        ## What's New

        See [CHANGELOG.md](CHANGELOG.md) for full details.

        ## Installation

        Download the IPA or build from source using Xcode 16+.

        ## Requirements

        - iOS 17.0 or later
        - iPhone or iPad
        EOF

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        body_path: release-notes.md
        draft: true
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
